# Supabase Database Configuration
# Get these from your Supabase project settings > Database > Connection string

# Connect to Supabase via connection pooling (Supavisor)
# Used for serverless deployments (Vercel) - Port 6543 (Transaction mode)
DATABASE_URL="postgres://[DB-USER].[PROJECT-REF]:[PRISMA-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database
# Used for migrations - Port 5432 (Session mode)
DIRECT_URL="postgres://[DB-USER].[PROJECT-REF]:[PRISMA-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"
